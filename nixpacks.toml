[phases.setup]
nixPkgs = ["nodejs_20", "python311"]

[phases.build]
cmds = [
  "cd frontend && npm install && npm run build && cd ..",
  "python3 -m venv .venv && .venv/bin/pip install -r backend/requirements.txt"
]

[start]
cmd = "cd backend && ../.venv/bin/gunicorn run:app --bind 0.0.0.0:$PORT"
